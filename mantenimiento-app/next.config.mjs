// next.config.mjs
import dotenv from 'dotenv';

// Cargar las variables de entorno
dotenv.config();

const nextConfig = {
  // Puedes añadir cualquier configuración de Next.js aquí
};

export default nextConfig;

